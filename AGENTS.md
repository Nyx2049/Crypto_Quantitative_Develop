# AI 协作约定

## 候选模块

- 页面中的“候选模块”是选股/选币策略卡片的容器。
- 候选卡片必须使用具体策略名加版本号命名。当前策略为 `EMA99 零度筛选器 2.0`。
- 新增候选策略时，默认复用现有卡片 UI，并按页面顺序向下添加，不要把页面扩展成复杂交易终端。
- 每张卡片要明确自身筛选或排序口径，只输出候选标的，不宣称确定进场或反转。
- 优先复用已有扫描数据，避免对相同行情发起重复请求。
- 新增或重命名策略时，同步更新 README 和相关测试。
- 2.0 保留近零 Top 20，并在当前扫描池内附加穿零后 72h 留观；1H 回踩状态不是入选硬门槛。穿零时间和结构参考价只能使用事件当时的已收盘数据，禁止使用未来 4H EMA 解释过去的 1H 行情。
- 新策略逻辑优先放在可测试的纯函数模块中；页面展示、阈值说明、单元测试和移动端浏览器测试必须一起更新。未做回测不得承诺胜率、盈亏比或更少止损。
- 本约定用于帮助后续 AI 和维护者保持一致，但不是写死的产品限制；如需求变化，可以同时修改实现、文档和测试。
